{
    "Comment": "Register User",
    "StartAt": "Create Setting",
    "States": {
        "Create Setting": {
            "Type": "Task",
            "Resource": "${CreateSettingsArn}",
            "ResultPath": "$.setting",
            "Next": "END CREATE"
        },
        "END CREATE": {
            "Type": "Pass", 
            "Parameters": {
                "setting": "$.setting"
            },
            "End": true
        }
        
    }
}